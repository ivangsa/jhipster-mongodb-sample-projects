version: '2'
services:
    jhipster-mongodb-embedded-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb jhipster-mongodb-embedded-mongodb-config
    jhipster-mongodb-embedded-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    jhipster-mongodb-embedded-mongodb-config:
        image: mongo:3.6.3
        container_name: jhipster-mongodb-embedded-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
